<h2>Category: Location</h2>
<ul>
	<li>
		<b>Location</b>:
		To choose a location for you object open the location browser in a new window. In it you will see a menu tree with all existing objects which had their category "Location" edited. Now choose an object from the tree and wait until it is loaded at the bottom of the window. Then click "Save" and the window will close.<br/>
		By default the following three objects are "containers": building, room, enclosure. In the object type configuration you also can define other/new objects as "containers".
	</li>
	<li>
		<b>GUI (only for a certain type of object):</b>
		<ul>
			<li><b>Position in the rack</b>: Positions in a rack are being counted from the bottom to the top and start with 1. Every position is in correlation to a "height unit".</li>
			<li><b>Front side of the rack</b>: Defines if an object is used at the front side or the back side of a rack. For example PDUs could be installed at the back side of a positioln where a server is already in place at the front side.</li>
			[{*<li><b>Image of the object in the rack</b>: Choose an image for your object which is then shown in WIMS. In order to do that WIMS's path has to be defined in the file "config.inc.php".</li>*}]
		</ul>
	</li>
</ul>